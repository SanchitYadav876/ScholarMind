services:
  - type: web
    name: scholarmind
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT
    rootDir: developing_AI
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
